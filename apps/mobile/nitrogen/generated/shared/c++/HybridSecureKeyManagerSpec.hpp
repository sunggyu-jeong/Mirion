///
/// HybridSecureKeyManagerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <NitroModules/Promise.hpp>
#include <NitroModules/Null.hpp>
#include <variant>

namespace margelo::nitro::lockfi::securekeymanager {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SecureKeyManager`
   * Inherit this class to create instances of `HybridSecureKeyManagerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSecureKeyManager: public HybridSecureKeyManagerSpec {
   * public:
   *   HybridSecureKeyManager(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSecureKeyManagerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSecureKeyManagerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSecureKeyManagerSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual bool hasPrivateKey(const std::string& keyId) = 0;
      virtual bool deletePrivateKey(const std::string& keyId) = 0;
      virtual std::shared_ptr<Promise<bool>> generateAndStorePrivateKey(const std::string& keyId) = 0;
      virtual std::shared_ptr<Promise<std::variant<nitro::NullType, std::string>>> retrievePrivateKey(const std::string& keyId) = 0;
      virtual std::shared_ptr<Promise<bool>> storeData(const std::string& keyId, const std::string& data) = 0;
      virtual std::shared_ptr<Promise<std::variant<nitro::NullType, std::string>>> retrieveData(const std::string& keyId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SecureKeyManager";
  };

} // namespace margelo::nitro::lockfi::securekeymanager
